[package]
name = "ling"
version = "0.1.0"
edition = "2021"
description = "A toy model exploring language and thinking through function spaces and operator learning"
authors = ["moxian"]

[dependencies]
num-complex = "0.4"
rand = "0.8"
rand_distr = "0.4"

[profile.release]
opt-level = 3
